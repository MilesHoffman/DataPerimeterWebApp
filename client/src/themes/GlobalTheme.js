import { createTheme } from '@mui/material/styles';

const globalTheme = createTheme({
	palette: {
		background: {
			default: '#F2F3F5', // Your desired background color
		},
	},
});

export default globalTheme;